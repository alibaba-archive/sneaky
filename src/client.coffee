###
sneaky clients
these clients need to be deployed on remote servers
###

async = require('async')
{cpuMonitor, memoryMonitor, diskMonitor} = require('./monitors')

class Client

  constructor: ->

  publish: ->

module.exports = Client