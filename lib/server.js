// Generated by CoffeeScript 1.6.3
/*
monitor server
this server will provide a web interface for server data
*/


(function() {
  var Logger, Server, express, http, port;

  express = require('express');

  http = require('http');

  port = 3356;

  Logger = require('./logger');

  Server = (function() {
    function Server() {
      this.logger = new Logger();
    }

    Server.prototype.serv = function() {
      var _this = this;
      this.app = express();
      this.server = http.createServer(this.app);
      this.app.configure(function() {
        _this.app.use(_this.logger.serv);
        _this.app.use(express.cookieParser());
        return _this.app.use(_this.app.router);
      });
      this.app.get('/', function(req, res) {
        return res.send('Welcome to Sneaky!');
      });
      this.server.listen(port);
      return this.logger.log("server listen on " + port);
    };

    Server.serv = function() {
      var server;
      server = new Server();
      server.serv();
      return server;
    };

    return Server;

  })();

  module.exports = Server;

}).call(this);
